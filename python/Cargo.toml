[package]
name = "engram-py"
version = "0.1.0"
edition = "2024"

[lib]
name = "engram"
crate-type = ["cdylib"]

[dependencies]
engram = { path = ".." }
pyo3 = { version = "0.21", features = ["extension-module"] }
numpy = "0.21"
